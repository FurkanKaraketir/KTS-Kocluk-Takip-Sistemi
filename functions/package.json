{
  "name": "kts-functions",
  "description": "KTS FCM HTTP v1 — deploy: firebase deploy --only functions",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "dependencies": {
    "firebase-admin": "^13.0.0",
    "firebase-functions": "^6.3.0"
  }
}
